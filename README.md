# Football Mini App - Telegram WebApp

Современное React-приложение для интернет-магазина футбольных футболок, интегрированное с Telegram WebApp.

## 🚀 Особенности

- **React 18** с TypeScript
- **Redux Toolkit** для управления состоянием
- **Framer Motion** для плавных анимаций
- **Tailwind CSS** для стилизации
- **Telegram WebApp API** интеграция
- **Mobile-first** дизайн
- **Responsive** интерфейс
- **Корзина и избранное** для каждого пользователя
- **Оформление заказов** с подтверждением через админ-панель
- **Автоматический пересчет остатков** при подтверждении заказов

## 🛠 Технологии

### Frontend
- React 18
- TypeScript
- Redux Toolkit
- Framer Motion
- Tailwind CSS
- Lucide React (иконки)
- Vite

### Backend
- Django 5.2
- Django REST Framework
- SQLite (для разработки)
- Poetry для управления зависимостями

## 📁 Структура проекта

```
├── front/                 # React фронтенд
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── store/        # Redux store
│   │   ├── hooks/        # Кастомные хуки
│   │   ├── services/     # API сервисы
│   │   └── data/         # Моковые данные
│   └── package.json
├── backend/              # Django бэкенд
│   ├── football_mini_app/
│   │   ├── users/        # Модели пользователей
│   │   ├── products/     # Модели товаров
│   │   ├── orders/       # Модели заказов
│   │   └── manage.py
│   └── pyproject.toml
└── README.md
```

## 🚀 Запуск проекта

### Backend (Django)

1. **Перейдите в папку backend:**
```bash
cd backend
```

2. **Установите Poetry (если не установлен):**
```bash
curl -sSL https://install.python-poetry.org | python3 -
```

3. **Установите зависимости:**
```bash
poetry install
```

4. **Активируйте виртуальное окружение:**
```bash
poetry shell
```

5. **Выполните миграции:**
```bash
cd football_mini_app
python manage.py migrate
```

6. **Создайте суперпользователя:**
```bash
python manage.py createsuperuser
```

7. **Запустите сервер:**
```bash
python manage.py runserver
```

Бэкенд будет доступен по адресу: http://localhost:8000
Админ-панель: http://localhost:8000/admin

### Frontend (React)

1. **Перейдите в папку front:**
```bash
cd front
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Запустите в режиме разработки:**
```bash
npm run dev
```

Фронтенд будет доступен по адресу: http://localhost:5173

## 🔧 Конфигурация

### Telegram WebApp

Приложение автоматически интегрируется с Telegram WebApp API:

```typescript
// Инициализация при загрузке
useEffect(() => {
  if (window.Telegram?.WebApp) {
    window.Telegram.WebApp.ready();
    window.Telegram.WebApp.expand();
  }
}, []);
```

### Переменные окружения

Создайте файл `.env.local` в папке front для настройки:

```env
VITE_API_URL=http://localhost:8000
VITE_TELEGRAM_BOT_TOKEN=your_bot_token_here
```

## 📱 Функционал

### Каталог товаров
- Отображение товаров в сетке
- Фильтрация по типу, производителю, лиге, сезону, состоянию, размеру
- Поиск по названию и описанию
- Сортировка и пагинация

### Корзина
- Добавление/удаление товаров
- Изменение количества
- Расчет общей стоимости
- Оформление заказа с формой доставки

### Избранное
- Добавление/удаление товаров в избранное
- Просмотр списка избранных товаров

### Заказы
- Оформление заказа из корзины
- Автоматическое создание заказа в статусе "Ожидает подтверждения"
- Подтверждение заказов через админ-панель
- Автоматический пересчет остатков товаров

## 🎨 Дизайн

### Цветовая схема
- **Primary**: Синий (#3B82F6)
- **Accent**: Желтый (#EAB308)
- **Gray**: Оттенки серого для текста и фона

### Типографика
- **Font**: Inter (Google Fonts)
- **Weights**: 300, 400, 500, 600, 700, 800

### Анимации
- Плавные переходы между состояниями
- Hover эффекты
- Анимации появления/исчезновения
- Spring анимации для интерактивных элементов

## 🔌 API интеграция

### Моковые данные

В `src/data/products.ts` находятся примеры товаров для тестирования:

```typescript
export const mockProducts: Product[] = [
  {
    id: 1,
    name: "Футболка Барселона 2023/24",
    team: "Барселона",
    brand: "Nike",
    price: 6500,
    // ... другие поля
  }
];
```

### Замена на реальный API

Для интеграции с реальным API замените импорты в компонентах:

```typescript
// Вместо моковых данных
import { mockProducts } from '../data/products';

// Используйте API
const { data: products } = useQuery(['products'], fetchProducts);
```

## 📱 Адаптивность

Приложение полностью адаптивно:

- **Mobile**: 1 колонка товаров
- **Tablet**: 2 колонки товаров
- **Desktop**: 3-4 колонки товаров
- **Touch-friendly**: Оптимизировано для мобильных устройств

## 🚀 Развертывание

### Backend (Django)

1. **Настройте production настройки в `settings_prod.py`**
2. **Настройте базу данных PostgreSQL**
3. **Соберите статические файлы:**
```bash
python manage.py collectstatic
```

4. **Запустите с Gunicorn:**
```bash
gunicorn football_mini_app.wsgi:application
```

### Frontend (Vercel)

```bash
npm install -g vercel
vercel --prod
```

### Netlify

```bash
npm run build
# Загрузите папку dist в Netlify
```

## 🔐 Админ-панель

### Управление заказами

В админ-панели доступны следующие функции:

- **Просмотр всех заказов** с детальной информацией
- **Подтверждение заказов** - автоматически уменьшает остатки товаров
- **Отмена заказов** - возвращает товары на склад
- **Изменение статуса** заказов (отправлен, доставлен)
- **Просмотр статистики** по пользователям

### Управление товарами

- **Добавление/редактирование** товаров
- **Управление остатками** и доступностью
- **Загрузка изображений** для товаров
- **Настройка фильтров** и категорий

### Управление пользователями

- **Просмотр статистики** по заказам, корзине и избранному
- **Управление правами** доступа
- **Просмотр активности** пользователей

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для деталей.

## 📞 Поддержка

Если у вас есть вопросы или предложения, создайте Issue в репозитории или свяжитесь с командой разработки.

## 🔧 Troubleshooting

### Проблемы с Telegram WebApp

1. **Проверьте, что приложение запущено в Telegram**
2. **Убедитесь, что бот настроен правильно**
3. **Проверьте консоль браузера на ошибки**

### Проблемы с API

1. **Проверьте, что бэкенд запущен**
2. **Убедитесь, что CORS настроен правильно**
3. **Проверьте логи Django сервера**

### Проблемы с базой данных

1. **Выполните миграции:**
```bash
python manage.py makemigrations
python manage.py migrate
```

2. **Создайте суперпользователя:**
```bash
python manage.py createsuperuser
```
